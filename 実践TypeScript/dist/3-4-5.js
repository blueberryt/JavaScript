"use strict";
function getScoreAmount(score) {
    switch (score) {
        case "A":
            return 100;
        case "B":
            return 60;
        case "C":
            return 30;
    }
}
